For the rating model I implemented a Latent Factor Model and by fine tuning the regularizers, as well as the n_epochs / n_factors

For the read model, I used a simple heuristic that relies on cosine similarity as well as the popularity of the book